{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run example app",
            "type": "dart",
            "request": "launch",
            "program": "${workspaceFolder}/example/lib/main.dart",
            "cwd": "${workspaceFolder}",
            "args": [
                "--platform=windows"
            ]
        }
    ]
}