name: audiopc
description: "A new Dart FFI package project."
version: 0.0.1

environment:
  sdk: ^3.11.1

dependencies:
  code_assets: ^1.0.0
  ffi: ^2.2.0
  hooks: ^1.0.0
  logging: ^1.3.0
  native_toolchain_rust: ^1.0.3

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.28.0
